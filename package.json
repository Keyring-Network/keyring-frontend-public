{
  "name": "keyring-frontend-public",
  "version": "1.0.0",
  "description": "This repository contains the public frontend code from Keyring Network.",
  "author": "Keyring Network",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  }
}