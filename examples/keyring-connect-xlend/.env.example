# Dev API override, also used for policy loading and extension verification.
NEXT_PUBLIC_KEYRING_API_BASE_URL=http://localhost:8000

# Server-only proof lookup keys. Never prefix these with NEXT_PUBLIC_.
KEYRING_DATA_SHARING_API_KEY_DEV=
KEYRING_DATA_SHARING_API_KEY_PROD=
